@fanvue:registry=https://registry.npmjs.org/
# pnpm settings. The lockfile records auto-install-peers, so keep it in sync with pnpm-lock.yaml.
# npm and npx print a harmless "Unknown project config" warning for it; ignore that.
auto-install-peers=false
update-notifier=false
